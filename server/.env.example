# Server Configuration
port PORT=3030

# Database Configuration
# IMPORTANT: Change these values for your environment
MYSQL_HOST=localhost
MYSQL_USER=your_database_user
MYSQL_PASSWORD=your_secure_password_here
MYSQL_DATABASE=your_database_name

# Application URLs
CLIENT_URL=https://your-domain.com
ALIAS_CLIENT_URL=https://www.your-domain.com
BACKEND_URL=https://your-domain.com/api

# Optional: Cookie Domain (uncomment if needed)
#COOKIE_DOMAIN=.your-domain.com

# Security
# Generate a strong JWT secret with: openssl rand -hex 32
JWT_SECRET=your_jwt_secret_generate_with_openssl_rand_hex_32
